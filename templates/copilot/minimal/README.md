# Copilot harness templates

Copy into `.github/` in your project. Pair with universal templates.

```bash
mkdir -p .github/instructions .github/agents
cp copilot-instructions.md ../your-repo/.github/
cp instructions/*.md ../your-repo/.github/instructions/
cp agents/*.md ../your-repo/.github/agents/
```
